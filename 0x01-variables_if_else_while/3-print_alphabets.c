#include <stdlib.h>
#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
/**
 * main - entry point
 *
 * Return: Success(0)
 */
int main(void)
{
	int n;

	ln = 'a';
	while (ln <= 'z')
	{
		putchar(ln);
		ln++;
	}
	Un = 'A';
	while (Un <= 'Z')
	{
		putchar(Un);
		Un++;
	}
	putchar('\n');
	return (0);
}
